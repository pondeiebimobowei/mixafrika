import { Text } from 'react-native';
import { ScrollView } from 'react-native';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function Esusu() {
  return (
    <SafeAreaView>
      <ScrollView>
        <Text>Esusu Page</Text>
      </ScrollView>
    </SafeAreaView>
  );
}
